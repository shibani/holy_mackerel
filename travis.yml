language: elixir

elixir:
  - 1.7.1

addons:
  postgresql: '10'

services:
  - postgresql

before_script:
  - cp src/config/travis.exs src/config/test.exs
  - mix do ecto.create, ecto.migrate

script:
  - mix test
